"use client";

//import { filterableItems, getColumns } from "./column";
//import { DataTable } from "@/components/shared/data-table";

export default function PageTable2() {
  {
    /*const tableContainerRef = useRef<HTMLDivElement>(null);

    /*const selectionOptions: ActionItem<DataTableUserStorybook>[] = dataTableSelectionActions.map(action => ({
        type: "action",
        ...action
    }));*/
  }
  {
    /*const handleChange = useCallback((data: DataTableUserStorybook[]) => {
        console.log("A selection is made");
        console.log(data);
    }, [])
    const columns = useMemo(() => getColumns(), []);
    return (
        <div className="flex flex-col p-2 md:p-4">
            <DataTable isPending={false}
                searchKey="username"
                onSelectedRowsChange={handleChange}
                filterableItems={filterableItems}
                tableContainerRef={tableContainerRef}
                selectionOptions={dataTableSelectionActions}
                columns={columns}
                data={dataTableDemousers} />
        </div>
    )*/
  }
  return <> </>;
}
