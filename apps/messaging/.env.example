# ─── AWS Identity ────────────────────────────────────────────────────────────
# For local development only. In CI/Lambda, use IAM roles (OIDC / execution role).
AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_PROFILE=default

# ─── CDK Bootstrap ───────────────────────────────────────────────────────────
CDK_DEFAULT_ACCOUNT=123456789012
CDK_DEFAULT_REGION=us-east-1

# ─── SQS ─────────────────────────────────────────────────────────────────────
# Populated by CDK output after first deploy; required by the enqueue client.
SQS_EMAIL_QUEUE_URL=https://sqs.us-east-1.amazonaws.com/123456789012/discolaire-email-queue

# ─── SES ─────────────────────────────────────────────────────────────────────
# Must be a verified SES identity (email or domain).
SES_FROM_ADDRESS=test@example.com
SES_REGION=us-east-1

# ─── Lambda runtime (set via CDK environment block, not .env) ────────────────
# IDEMPOTENCY_TABLE is injected by CDK into the Lambda environment.
# Listed here for local testing only.
IDEMPOTENCY_TABLE=example-email-idempotency

# ─── Stack naming ────────────────────────────────────────────────────────────
# Optional prefix for all CDK resource names (default: "discolaire").
STACK_PREFIX=example
